import React from 'react';
import {Button} from 'reactstrap';


const resumeContactDetails = (props) => {

    return(
      <div>
          firstPage
          <Button onClick={props.onSubmit}>NextPage</Button>
      </div>
    );
};
export default resumeContactDetails;